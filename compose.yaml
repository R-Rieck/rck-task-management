services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=taskmanagement'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'
